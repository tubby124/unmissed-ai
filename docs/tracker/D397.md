# D397 — Insurance Chip Slot Mapping

**Status:** ✅ Done  
**Priority:** P0  
**Date:** 2026-04-04

## Problem
The `insurance` chip collected during HVAC onboarding had no directMappings entry in prompt-slots.ts → silently dropped from every HVAC prompt.

## Fix
Added `insurance: 'INSURANCE_TYPE'` to directMappings in prompt-slots.ts.

## Notes
INSURANCE_TYPE is not referenced anywhere in the HVAC prompt block — only the directMappings entry was added. INSURANCE_TYPE did not exist anywhere in the codebase prior to this fix; the mapping registers it for future prompt template use.
