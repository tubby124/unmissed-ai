# D394 — GREETING_LINE Fallback + AhaMoment Skeleton State

**Status:** ✅ done (2026-04-04)
**Files:** `src/app/onboard/steps/step6-activate.tsx`, `src/app/api/onboard/generate-agent-intelligence/route.ts`

## What
Two fixes for GREETING_LINE reliability: loading skeleton in AhaMomentPanel + server-side fallback template if Haiku returns empty.

## Implementation

### AhaMomentPanel skeleton (step6-activate.tsx)
- Added `isGenerating = !greeting && !!(data.businessName && data.niche)`
- Inserted pulsing skeleton early-return before existing `if (!greeting) return null`
- Reconciled with existing `greeting` variable (no duplicate)

### GREETING_LINE fallback (generate-agent-intelligence/route.ts)
- Inserted after seed assembly, before TRIAGE_DEEP length validation
- Uses existing variables: `body.businessName` and `agentName` (local const)
- Template: `"Thank you for calling ${body.businessName}, this is ${agentName}. How can I help you today?"`

## Known Gap
Fallback is before the existing < 20 char validator at line 261. If Haiku returns a very short non-empty GREETING_LINE, the validator clears it AFTER the fallback ran. Tracked as D398 (move fallback to after validator).

## TypeScript
Zero errors.
