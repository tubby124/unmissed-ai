---
type: tracker-item
id: D233
status: done
priority: critical
tags: [tracker, ops, cron]
affects-clients: [all]
date-added: 2026-03-31
---

# D233 — Verify CRON_SECRET in Railway

## Problem
All 10 cron jobs silently fail if CRON_SECRET env var is not set in Railway.
Set in .env.local as `983d6f36...` but never confirmed in Railway dashboard.

## Solution
1. Open Railway dashboard → unmissed-ai service → Variables
2. Check if CRON_SECRET is present and matches .env.local value
3. If missing: add it now

## Why Critical
Silent failure — no error, no alert. Drift-check cron (D221), digest, all scheduled tasks: all broken.

## Status
Cannot verify via CLI in current session. Must be done in Railway dashboard manually.
