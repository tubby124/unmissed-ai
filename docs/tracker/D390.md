# D390 — Split step1-gbp: Move Voice Picker + Caller Reasons to StepNiche

**Status:** ✅ Done  
**Priority:** P1  
**Date:** 2026-04-04

## Problem
step1-gbp.tsx did 6 things: GBP lookup + niche detection + voice picker + caller reasons + background intelligence generation. NLM Q2 confirmed voice picker and caller reasons are agent config (not business lookup) and should live in the niche configuration step.

## Fix
- Moved voice picker and caller reasons UI from step1-gbp.tsx to step-niche.tsx
- Moved intelligence generation trigger to step-niche (fires after niche chips + voice + caller reasons via onBlur)
- step1-gbp now only handles: GBP lookup, manual entry, niche detection, agent name
- step-niche.tsx now renders: niche chip form → voice style picker → caller reasons inputs
- Intelligence trigger in step-niche retains D391 services-change useEffect + adds onBlur re-fire for caller reasons
- steps.ts canAdvance updated: voiceId gate moved from step 1 to step 2 (after voice picker is in step 2)
- Step labels confirmed correct: "Your business" → "Your agent" → "Your plan" → "Launch"
