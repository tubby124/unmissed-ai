---
type: tracker
status: not-started
priority: low
tags: [onboarding, investigation]
related: [[D317]]
updated: 2026-04-01
---

# D371 — Investigate owner_name source during onboarding

## Problem
During Electric Avenue Mushroom Dispensary onboarding, `owner_name` was set to "Emon" without the user intentionally entering it. Need to trace where this value gets populated — GBP scrape extraction, website scrape, or onboarding form default.

## Context
User (Hasan) doesn't recall entering "Emon". Could be:
1. GBP data extraction picking up a contact name
2. Website scrape extracting a name from the site
3. Accidental form input
4. Default from some lookup

## Investigation Steps
- [ ] Check `buildPromptFromIntake()` for where `owner_name` is set
- [ ] Check GBP extraction code for name extraction
- [ ] Check `toIntakePayload()` for owner_name mapping
- [ ] Check onboarding step components for the field

## Key Files
- `src/lib/intake-transform.ts` — `toIntakePayload()`
- `src/lib/prompt-builder.ts` — `buildPromptFromIntake()`
- Onboarding step components
