# D430 — No client-facing agent preview ("Test your agent" button)

**Status:** resolved
**Priority:** P2
**Filed:** 2026-04-06
**Resolved:** 2026-04-06

## Resolution
`AgentTestCard` component exists at `src/components/dashboard/AgentTestCard.tsx` and is rendered in `src/app/dashboard/agent/AgentPageView.tsx`. Full WebRTC call UI with transcript, call timer, waveform, rate prompt. Uses `useCallContext`. This was already shipped.

## Notes
Tracker was filed before AgentTestCard was built. Endpoint: multiple exist (`/api/trial/test-call`, `/api/dashboard/browser-test-call`, `/api/dashboard/test-call`).
