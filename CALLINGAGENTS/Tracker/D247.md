---
type: tracker-item
id: D247
status: done
priority: critical
tags: [tracker, onboarding, prompt, intent, haiku]
related: [Tracker/D243, Tracker/D241, Tracker/D254, Product/Intent Classification, Product/Working Agent Patterns, Architecture/Prompt Generation]
updated: 2026-03-31
---

## Implementation (2026-03-31) — DONE ✅

**Build verified: `✓ Compiled successfully in 6.2s`**

### Changes made
1. `src/types/onboarding.ts` — added `callerReasons?: string[]` field
2. `src/app/api/onboard/infer-niche/route.ts` — new D247 path: when `callerReasons + knownNiche` in body, skip niche classification and generate custom TRIAGE_DEEP via Haiku for ANY niche. Returns `{niche, customVariables: {TRIAGE_DEEP}}`.
3. `src/app/onboard/steps/step3-capabilities.tsx` — added "Why do people call you?" section with 3 text inputs. On blur, calls `infer-niche` with `{businessName, knownNiche, callerReasons}`. Stores returned TRIAGE_DEEP in `data.nicheCustomVariables`. Shows loading + success state.
4. **Critical bug fixed in `src/lib/prompt-builder.ts`** — `niche === 'other'` guard was blocking TRIAGE_DEEP from reaching any non-'other' niche. Removed guard so `niche_custom_variables` applies to ALL niches.
5. `src/lib/settings-schema.ts` — added `niche_custom_variables` to FIELD_REGISTRY (`DB_ONLY`) and Zod schema.
6. `src/app/dashboard/settings/page.tsx` — added `niche_custom_variables` to `ClientConfig` interface and SELECT string.

**Companion: [[Tracker/D254]] — post-onboarding dashboard card for editing caller reasons + regenerating TRIAGE_DEEP.**

### Flow
```
Owner fills "why do people call?" in step3 onboarding
    → blur → POST /api/onboard/infer-niche with callerReasons + knownNiche
    → Haiku generates TRIAGE_DEEP specific to those 3 reasons
    → stored in OnboardingData.nicheCustomVariables.TRIAGE_DEEP
    → toIntakePayload() → niche_custom_variables
    → buildPromptFromIntake() line 664-669: replaces ## 3. TRIAGE section
    → live agent has custom intent routing from day 1
```

### Remaining (not blocking)
- [ ] Test: fill in 3 reasons for auto_glass business → verify TRIAGE_DEEP generated matches reasons
- [ ] Verify the TRIAGE_DEEP reaches the provisioned agent prompt (check prompt after trial provision)
- [ ] Update `PROVISIONING/app/prompt_builder.py` for parity (D240-DEPLOY)

# D247 — Owner Intent → Outcome Mapping in Onboarding

## The Problem
Every new client gets a generic info bot because onboarding never asks WHY people call.
The 4 working agents only work because Hasan manually answered ~6 questions per client.
This systematizes that manual work.

## Source Research
Full pattern analysis: `memory/working-agent-patterns.md`
The 6 questions that replicate Hasan's manual work:

1. "What are the top 2-3 things callers ask when they call?"
   → intent buckets in TRIAGE_DEEP

2. "For each of those — what's the ONE thing you need from them before you can help?"
   → `→ Collect:` line per intent + the critical niche question

3. "What tells you a call is urgent vs. routine?"
   → URGENT block + auto-urgent skip list

4. "When a call ends well, what happened? (booked, gave a quote, took a message?)"
   → `→ Outcome:` line per intent bucket

5. "What should your agent NEVER say or commit to?"
   → NEVER block in CORE RULES

6. "Are there different types of callers who need different treatment?" (optional)
   → tiered info collection + caller-type shortcuts

## What Haiku Generates From These
From answers → Haiku generates:
- custom `TRIAGE_DEEP` (intent buckets with trigger keywords + Collect + Outcome)
- `INFO_TO_COLLECT` tiered per caller type
- `CLOSE_ACTION` per intent (book / quote callback / message + named owner)
- urgency keywords list
- NEVER block

## Code Path (all already exists from D240/D241)
```
onboarding step3 answers
  → intake-transform.ts → niche_custom_variables
  → infer-niche/route.ts (Haiku) → TRIAGE_DEEP
  → prompt-builder.ts → buildPromptFromIntake() → system_prompt
  → createAgent() → live Ultravox agent
```

The D240 propagation bug is fixed — `prompt-builder.ts` now reads `variables.TRIAGE_DEEP` not `nicheDefaults.TRIAGE_DEEP`.

## Acceptance
- new client onboards → answers 3 reasons → Haiku generates TRIAGE_DEEP
- provisioned prompt contains intent buckets with trigger keywords, Collect, Outcome per bucket
- test call confirms intent routing works (doesn't interrogate for name before identifying intent)
