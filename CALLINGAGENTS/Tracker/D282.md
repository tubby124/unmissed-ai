---
type: tracker
status: done
priority: HIGH
tags: [gap, prompt-patcher, business-name, phase4]
related: [[D281]], [[D260]], [[D274]]
updated: 2026-03-31
completed: 2026-03-31
---

# D282 — Business name change patches prompt + Ultravox sync

## Problem
Mutation contract listed `business_name` as `DB_ONLY` — post-provision edits didn't update the live prompt.

## Finding
**Already implemented in code!** `settings-patchers.ts` already handles business_name via `patchBusinessName()` (word-boundary replace). FIELD_REGISTRY already had `triggersPatch: 'business_name'`. The mutation contract was stale.

## Fix (Phase 4)
1. Updated mutation contract: `business_name` → `DB_PLUS_PROMPT` (was incorrectly listed as `DB_ONLY`)
2. Updated Risk 6 in mutation contract: marked RESOLVED
3. Verified chain: body.business_name → patchBusinessName() → system_prompt changes → computeNeedsSync → syncToUltravox

## No code change needed — just documentation alignment.
