---
type: tracker-item
id: D235
status: done
priority: medium
tags: [tracker, knowledge, bug]
affects-clients: [all pgvector clients]
related-features: [Features/Knowledge RAG]
date-added: 2026-03-31
date-completed: 2026-03-31
---

# D235 — Knowledge Reseed Gate Removal

## Problem
`reseedKnowledgeFromSettings()` originally had a guard that skipped reseeding when knowledge_chunks already existed.

## Resolution
**Already fixed** in commit `3d8b625` (SCRAPE7: stale chunk cleanup + consolidate knowledge seeding paths).

Current code in `src/lib/embeddings.ts` lines 224-255:
- Line 236: `await deleteClientChunks(clientId, 'settings_edit')` runs **unconditionally** before re-embedding
- Line 238-240: Early return only if no new chunks to embed (delete still happened)
- No conditional guard on existing chunks exists in current code

Verified 2026-03-31 during Phase 1 of prompt architecture refactor.

## File
`src/lib/embeddings.ts` — reseedKnowledgeFromSettings()

## Connections
- → [[Features/Knowledge RAG]] (silent divergence resolved)