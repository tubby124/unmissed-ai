---
type: tracker
id: D317
title: Verify personality preset round-trip (onboarding → DB → prompt → dashboard)
priority: medium
status: open
created: 2026-04-01
related:
  - "[[Architecture/Phase7-Onboarding-Audit]]"
  - "[[Features/Voice and Personality]]"
tags: [bug, personality, prompt, settings]
---

# D317 — Verify personality preset round-trip

## Issue
Agent personality is selected during onboarding (4 presets) but it's unclear if:
1. Dashboard settings can change it (VoiceStyleCard exists but needs verification)
2. Changing it on dashboard triggers `patchVoiceStyleSection()` → prompt repatch → Ultravox sync
3. The 4 onboarding presets match what the dashboard shows

## Data flow (confirmed)
```
Onboarding: data.agentTone (step2-voice-preview.tsx:257)
  → provision/trial/route.ts:170 → clients.voice_style_preset
    → prompt-slots.ts:891 → VOICE_PRESETS[presetId] → prompt text
```

## Verification needed
1. Does `PATCH /api/dashboard/settings` accept `voice_style_preset`?
2. Does changing it trigger `needsAgentSync`?
3. Does `patchVoiceStyleSection()` in prompt-patcher.ts work correctly?
4. Do the 4 preset IDs match between onboarding and dashboard?

## Files
- `src/app/onboard/steps/step2-voice-preview.tsx` (onboarding presets)
- `src/lib/prompt-slots.ts:891` (prompt injection)
- `src/app/api/dashboard/settings/route.ts` (settings PATCH)
- `src/lib/prompt-patcher.ts` (voice style patcher)
