---
type: tracker-item
id: D440
status: in-progress
priority: high
tags: [tracker, learning-bank, prompt-platform]
affects-clients: [all]
related-features: [Features/Learning-Bank]
date-added: 2026-04-29
---

# D440 — Learning Bank platform: extend call_insights into actionable feedback loop

## Problem
`call_insights` had 26 rows of useful signal (loop_rate, repeated_questions, caller_frustrated, agent_confidence) with no consumer. `knowledge_query_log` had 261 rows of unresolved gaps with no consumer. Every prompt improvement was a manual one-off; cross-niche learnings stayed locked inside individual prompts.

## Solution
Build a Learning Bank: 4 new tables (`prompt_patterns`, `prompt_lessons`, `pattern_application_log`, `call_transcripts`) + 1 view (`v_active_patterns_by_niche`). Auto-generate lessons from `call_insights` thresholds at `/completed`. Promote validated lessons into a reusable pattern library. Inject promoted patterns into `recomposePrompt()` (gated, default OFF until per-niche validated).

## Files
- supabase/migrations/20260429000000_create_learning_bank.sql
- supabase/migrations/20260429010000_create_call_transcripts.sql
- supabase/migrations/20260429020000_seed_prompt_patterns.sql
- src/lib/call-transcripts.ts
- src/lib/lesson-generator.ts
- src/app/api/webhook/[slug]/completed/route.ts (lines 474-501)
- src/app/dashboard/admin/learning-bank/page.tsx
- src/app/api/admin/learning-bank/{lessons,promote,patterns}/route.ts
- src/lib/slot-regenerator.ts (gated injection)
- scripts/backfill-transcripts.ts
- ~/.claude/skills/learn/SKILL.md + scripts/

## Dependencies
- Blocked by: nothing
- Blocks: D243, D244, D270, D284, D297 (all become trivial after this lands)

## Notes
- Migrations written but NOT applied to remote Supabase yet — Hasan applies via `supabase db push`.
- 14 cross-niche patterns + 10 niche one-liners pre-seeded with `status='promoted'`.
- `LEARNING_BANK_INJECT=true` env flag enables pattern injection — default OFF.
- Companion: D441 (Zara rewrite).
- `call_insights.loop_rate` column doesn't exist; substituted `repeated_questions >= 3 OR agent_confused_moments >= 2` as proxy throughout.
- See [[Decisions/2026-04-29-Learning-Bank]] for full reasoning.
