---
type: tracker-item
id: D296
status: done
priority: critical
tags: [tracker, bug, prompt-builder, phase3]
affects-clients: [new clients]
wave: 3
date-added: 2026-03-31
updated: 2026-03-31
---

# D296 — FORBIDDEN_EXTRA Dead Code Fix

**Status:** ✅ DONE (Phase 3)

## Problem
`niche_intake_modifications` in `prompt-builder.ts` (line 573) read from `nicheDefaults` instead of the actual `variables` object. This meant niche intake modifications were silently discarded — a dead-code bug.

## Fix
Auto-fixed by the slot builder refactor in Phase 3. The slot system reads variables correctly from `buildSlotContext()`, making the old `niche_intake_modifications` path obsolete.

## Note
The vault previously had this D-number assigned to "Agent Health Score Dashboard" — that was a D-number collision. Agent Health Score Dashboard has no D-number assigned yet.
