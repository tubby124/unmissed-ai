# D392 — Wire p1Triggers/emergencyTriggers → URGENCY_KEYWORDS

**Status:** ✅ done (2026-04-04)
**Files:** `src/lib/intake-transform.ts`, `src/lib/prompt-slots.ts`

## What
Wire niche chip values `p1Triggers` and `emergencyTriggers` from `nicheAnswers` into the `urgency_keywords` intake field, with human-readable label mapping. Add `urgency_keywords → URGENCY_KEYWORDS` to `directMappings` in prompt-slots.ts.

## Implementation
- Added `urgency_keywords` IIFE in `intake-transform.ts` after `services_not_offered`
- Merges `[...p1Triggers, ...emergencyTriggers]`, maps through 17-entry labelMap (burst_pipe, flooding, no_water, sewage_backup, gas_smell, ceiling_dripping, no_heat, no_heat_winter, furnace_not_starting, carbon_monoxide, no_cooling_extreme_heat, system_flooded, sparking, security, no_hot_water, elevator_stuck, fire)
- Added `urgency_keywords: 'URGENCY_KEYWORDS'` to `directMappings` in `prompt-slots.ts` (was absent)
- Also added `diagnostic_fee: 'DIAGNOSTIC_FEE'` to `directMappings` as gap fix (needed for HVAC TRIAGE_DEEP `{{DIAGNOSTIC_FEE}}` slot)

## TypeScript
Zero errors.
