---
type: tracker
id: D339
title: "Intelligence seed: CLOSE_PERSON from owner_name + greeting override verification"
priority: high
status: open
created: 2026-04-01
related:
  - "[[Architecture/Intelligence-Pipeline-Test-Findings]]"
  - "[[Architecture/Phase7-75-Second-Agent]]"
tags: [intelligence-pipeline, prompt, provision, bug-fix]
---

# D339 — CLOSE_PERSON from owner_name + greeting override verification

## What was done (this session)
- `provision/trial/route.ts`: Added `CLOSE_PERSON` derivation from `ownerName` first name
- `prompt-slots.ts`: Added `customVars.GREETING_LINE` priority over static niche wow greetings
- `generate-agent-intelligence/route.ts`: New endpoint created, handles both string and array TRIAGE_DEEP from Haiku

## What still needs verification
1. **Build passes** ✅ (verified)
2. **Dev server test**: Start dev, go through onboarding with a test business, verify:
   - Intelligence seed fires after GBP confirm (check Network tab for `/api/onboard/generate-agent-intelligence`)
   - `nicheCustomVariables` in localStorage has TRIAGE_DEEP, GREETING_LINE, URGENCY_KEYWORDS, FORBIDDEN_EXTRA
   - AgentIntelligenceCard renders on Launch step with intent count + expandable detail
   - After provision: `clients.niche_custom_variables` in Supabase has all 4 seed fields + CLOSE_PERSON
   - Generated `system_prompt` contains the intelligence seed content (not niche defaults)
3. **Promptfoo re-run**: After CLOSE_PERSON fix, re-run `sals-ny-pizza-test.yaml` — complaint tests should now say "Sal" not "the team"

## Files changed
- `src/app/api/onboard/generate-agent-intelligence/route.ts` (NEW)
- `src/app/onboard/steps/step1-gbp.tsx`
- `src/app/onboard/steps/step6-activate.tsx`
- `src/lib/prompt-slots.ts` (line 923-925)
- `src/app/api/provision/trial/route.ts` (CLOSE_PERSON)
- `src/types/onboarding.ts`
