# D395 — StepNiche canAdvance: Require ≥1 Service Chip

**Status:** ✅ done (2026-04-04)
**Files:** `src/app/onboard/config/steps.ts`

## What
Require at least one service chip selected before user can advance past StepNiche, for niches that have a services chip.

## Implementation
Replaced `canAdvance` on StepNiche step entry with services-gating logic for: auto_glass, plumbing, hvac, dental, legal, salon.
`OnboardingData` was already imported — no new import needed.

## Known Gap
`dental`, `legal`, `salon` are in nichesWithServices but those niche UIs don't exist yet. canAdvance will permanently block for those niches when selected. Low risk now (not selectable in picker), but needs cleanup before they go live.

## TypeScript
Zero errors.
